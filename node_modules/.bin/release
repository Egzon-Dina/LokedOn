#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../release-script/bin/release.js" "$@"
  ret=$?
else 
  node  "$basedir/../release-script/bin/release.js" "$@"
  ret=$?
fi
exit $ret
