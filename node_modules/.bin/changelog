#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../mt-changelog/bin/changelog" "$@"
  ret=$?
else 
  node  "$basedir/../mt-changelog/bin/changelog" "$@"
  ret=$?
fi
exit $ret
